import Vue from "vue";
import Router from "vue-router";
// import projectList from "../view/project/App.vue";
// import notFound from "../components/noFound.vue";

Vue.use(Router);
export default new Router({
  mode: "history",
  routes: [
    // {
    //   path: "/project",
    //   component: projectList
    // },
    // {
    //   path: "*",
    //   component: notFound
    // }
  ]
});
